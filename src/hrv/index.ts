/**
 * HRV subpath placeholder (Phase 0b builds this out): time/frequency/
 * nonlinear/ANS metrics on IBI arrays, plus respiration estimators. Empty
 * export for now so `ppg-js/hrv` resolves to a real module and dist/hrv/
 * exists as a build target.
 */
export {};
