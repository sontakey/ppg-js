/**
 * Source contract placeholder (Phase 1 builds CameraSource, ArraySource,
 * FileSource, BleHeartRateSource, MotionSource out fully). PPGMonitor still
 * owns its own getUserMedia flow directly for this phase; sources/ exists so
 * the directory and import path are stable ahead of that refactor.
 */
export {};
